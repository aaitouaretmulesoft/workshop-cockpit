# Vercel Postgres connection string
# Pull these automatically from Vercel with: vercel env pull .env
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Optional: protects the seed API route
SEED_SECRET=change-me

# Optional: gates the /admin page
ADMIN_PASSWORD=changeme
